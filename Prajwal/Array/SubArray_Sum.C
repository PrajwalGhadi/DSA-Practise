/*
Input:
N = 5, S = 12
A[] = {1,2,3,7,5}
Output: 2 4
*/

#include<stdio.h>
#include<stdlib.h>


int main(){

    // Declaring size of array and Target Variable
    int array_size, Target;

    printf("Enter the Size Array & Target:\n");
    scanf("%d\n %d", &array_size, &Target);

    //Declaring array 
    int array[array_size];

    for (int i = 0; i < array_size; i++){
        printf("Array[%d] = ", i);
        scanf("%d", &array[i]);
    }

    for (int i = 0; i < array_size; i++){
        for (int j = 0; j < array_size; j++){

            if (arr)
        }
    }
    return 0;
}